//+------------------------------------------------------------------+\n//|                                                      XAUUSD_EA.mq5|\n//|                        Expert Advisor for Trading XAUUSD         |\n//|                                                                  |\n//+------------------------------------------------------------------+\n#property strict\n\n// Parameters\ninput double LotSize = 0.1;          // Lot size for trading\ninput double TakeProfit = 50;         // Take profit in points\ninput double StopLoss = 50;           // Stop loss in points\ninput double TrailingStop = 20;       // Trailing stop in points\ninput double RiskPercentage = 1.0;    // Risk percentage per trade\n\n// Global variables\ndouble BuyPrice;\ndouble SellPrice;\n\n//+------------------------------------------------------------------+\n//| Expert initialization function                                   |\n//+------------------------------------------------------------------+\nint OnInit()\n{\n    // Initialization code\n    Print("XAUUSD Expert Advisor started.");\n    return INIT_SUCCEEDED;\n}\n\n//+------------------------------------------------------------------+\n//| Expert deinitialization function                                 |\n//+------------------------------------------------------------------+\nvoid OnDeinit(const int reason)\n{\n    // Cleanup code\n    Print("XAUUSD Expert Advisor stopped.");\n}\n\n//+------------------------------------------------------------------+\n//| Expert tick function                                             |\n//+------------------------------------------------------------------+\nvoid OnTick()\n{\n    // Automated trade management\n    ManageTrades();\n}\n\n//+------------------------------------------------------------------+\n//| Manual trade function                                            |\n//+------------------------------------------------------------------+\nvoid ManualTrade(bool buy)\n{\n    double price;\n    double riskAmount = AccountBalance() * RiskPercentage / 100;\n\n    if (buy)\n    {\n        price = Ask;\n        double sl = price - StopLoss * Point;\n        double tp = price + TakeProfit * Point;\n        \n        OrderSend(Symbol(), OP_BUY, LotSize, price, 2, sl, tp, "Manual Buy", MAGIC_NUMBER, 0, clrGreen);\n    }\n    else\n    {\n        price = Bid;\n        double sl = price + StopLoss * Point;\n        double tp = price - TakeProfit * Point;\n        \n        OrderSend(Symbol(), OP_SELL, LotSize, price, 2, sl, tp, "Manual Sell", MAGIC_NUMBER, 0, clrRed);\n    }\n}\n\n//+------------------------------------------------------------------+\n//| Manage existing trades                                           |\n//+------------------------------------------------------------------+\nvoid ManageTrades()\n{\n    // Loop through open orders and manage them\n    for (int i = OrdersTotal() - 1; i >= 0; i--)\n    {\n        if (OrderSelect(i, SELECT_BY_POS) && OrderSymbol() == Symbol())\n        {\n            // Implement trailing stop\n            if (OrderType() == OP_BUY)\n            {\n                if (Bid - OrderOpenPrice() > TrailingStop * Point)\n                {\n                    double newStopLoss = Bid - TrailingStop * Point;\n                    OrderModify(OrderTicket(), OrderOpenPrice(), newStopLoss, OrderTakeProfit(), 0, clrGreen);\n                }\n            }\n            else if (OrderType() == OP_SELL)\n            {\n                if (OrderOpenPrice() - Ask > TrailingStop * Point)\n                {\n                    double newStopLoss = Ask + TrailingStop * Point;\n                    OrderModify(OrderTicket(), OrderOpenPrice(), newStopLoss, OrderTakeProfit(), 0, clrRed);\n                }\n            }\n        }\n    }\n}\n\n//+------------------------------------------------------------------+